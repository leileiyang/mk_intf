
# Note: emcsvr is the master for all NML channels, and therefore is the
# first to start.

# Buffers
# Name                  Type    Host            size    neut?   (old)   buffer# MP ---

# Top-level buffers to EMC
B emcCommand            SHMEM   192.168.0.107       8192    0       0       1       16 1001 TCP=5005 xdr queue
B emcStatus             SHMEM   192.168.0.107       16384   0       0       2       16 1002 TCP=5005 xdr
B emcError              SHMEM   192.168.0.107       8192    0       0       3       16 1003 TCP=5005 xdr queue


# Processes
# Name          Buffer          Type    Host            Ops     server? timeout master? cnum

P xemc          emcCommand      REMOTE   localhost       W       0       10.0    0       10
P xemc          emcStatus       REMOTE   localhost       R       0       10.0    0       10
P xemc          emcError        REMOTE   localhost       R       0       10.0    0       10
